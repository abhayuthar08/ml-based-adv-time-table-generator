const transformToMLFormat = (data) => {
    // Convert input data into the appropriate format required by the machine learning model
    return {
      transformedData: {
        // Convert fields as needed
      }
    };
  };
  
  module.exports = { transformToMLFormat };
  